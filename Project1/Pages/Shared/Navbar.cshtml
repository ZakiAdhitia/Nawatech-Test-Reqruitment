<link href="~/css/navbar.css" rel="stylesheet" />
<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm mb-4">
    <div class="container">
        <a class="navbar-brand d-flex align-items-center" href="/Welcome">
            <span class="brand-name-nav">Welcome</span>
        </a>
        
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link nav-link-modern @(Context.Request.Path.StartsWithSegments("/Product") ? "active" : "")" href="/Product">Product</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link nav-link-modern @(Context.Request.Path.StartsWithSegments("/ProductCategory") ? "active" : "")" href="/ProductCategory">ProductCategory</a>
                </li>
            </ul>
            
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link nav-link-modern @(Context.Request.Path.StartsWithSegments("/Profile") ? "active" : "")" href="/Profile">
                        <i class="user-icon me-1"></i>Profile
                    </a>
                </li>
                <li class="nav-item">
                    <button class="btn btn-logout" onclick="logout()">
                        <i class="logout-icon me-1"></i>Logout
                    </button>
                </li>
            </ul>
        </div>
    </div>
</nav>

<script>
    function logout() {
        localStorage.removeItem("authToken");
        window.location.href = "/Auth/Login";
    }
</script>